// import { Product } from '../interfaces/product.interface';
// import { User } from '../interfaces/user.interface';

// export const validateBeforeSave = (item: Product | User): boolean => {
//     if (item as Product){
//        if (validateProduct(item)){
//             return true;
//         } 
//     }
// };

// const validateProduct = (item: Product) => {
//     if (item && item.name && item.price && item.count && item.tags){
//         return true;
//     }
// };
// //----------

// export const validateBeforeUpdate = (item: Product): boolean => {
//     if (item.name && item.price && item.count && item.tags){
//         return true;
//     }
// };

// export const validateBeforeDelete = (item: Product): boolean => {
//     if (item.name && item.price && item.count && item.tags){
//         return true;
//     }
// };